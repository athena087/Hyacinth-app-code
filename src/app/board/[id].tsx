// Route: "/board/[id]" → a saved board's detail grid. A root route (outside the
// tabs) so it pushes over Profile and back returns there. Implementation lives
// in src/screens/board.
export { default } from '../../screens/board/BoardDetailScreen';
