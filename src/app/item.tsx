// Route: "/item" → View Item screen. Sits outside (tabs) so it pushes as a
// full-screen page over whichever tab opened it — so back returns to the
// origin (Home feed or Results). Implementation lives in src/screens/item.
export { default } from '../screens/item/ViewItemScreen';
