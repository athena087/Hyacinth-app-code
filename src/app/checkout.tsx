// Route: "/checkout" → Checkout screen. Sits outside (tabs) so it pushes as a
// full-screen page with its own back button and no tab bar. Implementation
// lives in src/screens/checkout.
export { default } from '../screens/checkout/CheckoutScreen';
