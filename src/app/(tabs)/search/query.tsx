// Route: "/search/query" → the recent/type search screen, pushed within the
// Search tab's stack. Implementation lives in src/screens/search.
export { default } from '../../../screens/search/SearchQueryScreen';
