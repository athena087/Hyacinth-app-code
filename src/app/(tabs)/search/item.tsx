// Route: "/search/item" → View Item screen, pushed within the Search tab's
// stack (over results). Implementation lives in src/screens/item.
export { default } from '../../../screens/item/ViewItemScreen';
